---
name: Main

on: [push]

jobs:
  dependencies:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Setup node_modules cache
        uses: actions/cache@v1
        with:
          path: node_modules
          key: ${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Setup web/node_modules cache
        uses: actions/cache@v1
        with:
          path: web/node_modules
          key: ${{ runner.os }}-${{ hashFiles('web/package-lock.json') }}

      - name: Setup studio/node_modules cache
        uses: actions/cache@v1
        with:
          path: studio/node_modules
          key: ${{ runner.os }}-${{ hashFiles('studio/package-lock.json') }}

      - run: npm ci

  test:
    runs-on: ubuntu-latest
    needs: [dependencies]

    steps:
      - uses: actions/checkout@v2

      - name: Restore node_modules cache
        uses: actions/cache@v1
        with:
          path: node_modules
          key: ${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Restore web/node_modules cache
        uses: actions/cache@v1
        with:
          path: web/node_modules
          key: ${{ runner.os }}-${{ hashFiles('web/package-lock.json') }}

      - name: Restore studio/node_modules cache
        uses: actions/cache@v1
        with:
          path: studio/node_modules
          key: ${{ runner.os }}-${{ hashFiles('studio/package-lock.json') }}

      - run: npm test

  lint:
    runs-on: ubuntu-latest
    needs: [dependencies]

    steps:
      - uses: actions/checkout@v2

      - name: Restore node_modules cache
        uses: actions/cache@v1
        with:
          path: node_modules
          key: ${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Restore web/node_modules cache
        uses: actions/cache@v1
        with:
          path: web/node_modules
          key: ${{ runner.os }}-${{ hashFiles('web/package-lock.json') }}

      - name: Restore studio/node_modules cache
        uses: actions/cache@v1
        with:
          path: studio/node_modules
          key: ${{ runner.os }}-${{ hashFiles('studio/package-lock.json') }}

      - run: npm run lint

  deploy-graphql:
    runs-on: ubuntu-latest
    needs: [test, lint]
    if: github.ref == 'refs/heads/master'
    env:
      SANITY_AUTH_TOKEN: ${{ secrets.SANITY_AUTH_TOKEN }}

    steps:
      - uses: actions/checkout@v2

      - name: Restore node_modules cache
        uses: actions/cache@v1
        with:
          path: node_modules
          key: ${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Restore web/node_modules cache
        uses: actions/cache@v1
        with:
          path: web/node_modules
          key: ${{ runner.os }}-${{ hashFiles('web/package-lock.json') }}

      - name: Restore studio/node_modules cache
        uses: actions/cache@v1
        with:
          path: studio/node_modules
          key: ${{ runner.os }}-${{ hashFiles('studio/package-lock.json') }}

      - name: Deploy Graphql Server
        run: npm run graphql-deploy
        working-directory: ./studio
